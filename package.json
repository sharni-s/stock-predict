{
  "name": "stock-forecast",
  "version": "1.0.0",
  "description": "Stock forecasting with distributed and parallel SARIMAX",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sharni",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.10",
    "mqtt": "^4.2.8"
  }
}
